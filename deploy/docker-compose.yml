version: '2.0'
services:
  polymer:
    image: net:1.0
    container_name: polymer
    ports:
      - 8080:8080
    environment:
      - spring.profiles.active=prod
    volumes:
      - /work/www/polymer/polymer-application.jar:/app/application.jar
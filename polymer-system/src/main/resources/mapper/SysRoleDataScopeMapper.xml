<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.polymer.system.mapper.SysRoleDataScopeMapper">

    <resultMap type="SysRoleDataScopeEntity" id="SysRoleDataScopeResult">
        <result property="id" column="id"/>
        <result property="roleId" column="role_id"/>
        <result property="deptId" column="dept_id"/>
        <result property="creator" column="creator"/>
        <result property="createTime" column="create_time"/>
        <result property="updater" column="updater"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectSysRoleDataScopeVo">
        select id,
               role_id,
               dept_id,
               creator,
               create_time,
               updater,
               update_time
        from sys_role_data_scope
    </sql>

    <!--查询角色数据权限-->
    <select id="selectSysRoleDataScopeById" parameterType="Long" resultMap="SysRoleDataScopeResult">
        <include refid="selectSysRoleDataScopeVo"/>
        where id = #{id}
    </select>

    <!--新增角色数据权限-->
    <insert id="insertSysRoleDataScope" parameterType="SysRoleDataScopeEntity" useGeneratedKeys="true" keyProperty="id">
        insert into sys_role_data_scope
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="roleId != null">role_id,</if>
            <if test="deptId != null">dept_id,</if>
            <if test="creator != null">creator,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updater != null">updater,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="roleId != null">#{roleId},</if>
            <if test="deptId != null">#{deptId},</if>
            <if test="creator != null">#{creator},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updater != null">#{updater},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <!--修改角色数据权限-->
    <update id="updateSysRoleDataScope" parameterType="SysRoleDataScopeEntity">
        update sys_role_data_scope
        <set>
            <if test="id != null">id = #{id},</if>
            <if test="roleId != null">roleId = #{roleId},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="creator != null">creator = #{creator},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updater != null">updater = #{updater},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

    <!--删除角色数据权限-->
    <delete id="deleteSysRoleDataScopeById" parameterType="Long">
        delete from sys_role_data_scope where id = #{id}
    </delete>

    <!--根据角色ID，获取部门ID列表-->
    <select id="selectdeptIdsByRoleId" resultType="Long">
        select dept_id from sys_role_data_scope where role_id = #{roleId}
    </select>

    <!--根据角色id集合和部门id集合删除角色数据权限-->
    <delete id="deleteByRoleIdAnddeptId" parameterType="Long">
        delete from sys_role_data_scope
        <where>
            <if test="roleIdList != null and !roleIdList.isEmpty()">
                and role_id in
                <foreach collection="roleIdList" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="deptIdList != null and !deptIdList.isEmpty()">
                and dept_id in
                <foreach collection="deptIdList" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <!-- 兜底条件：两个列表都为空时阻止全表更新 -->
            <if test="(roleIdList == null or roleIdList.isEmpty())
                  and (deptIdList == null or deptIdList.isEmpty())">
                AND id = -1
            </if>
        </where>
    </delete>

</mapper>